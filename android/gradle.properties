org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# 2026-09-03: nonaktifkan Kotlin incremental — cache-nya crash
# ("different roots") karena source plugin di Pub cache (C:) beda drive
# dengan proyek (E:). Lihat ERROR.md §6.
kotlin.incremental=false
# 2026-09-03: batasi worker paralel — RAM mesin 8GB, build OOM
# (hs_err_pid9784.log: malloc gagal). Lihat ERROR.md §6.
org.gradle.workers.max=2
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
